/**
 * Approval queue filter — matches the existing Hijra Bank ESS routes
 * (`/approvals`, `/approvals/approved`, `/approvals/rejected`).
 */
export type ApprovalListType = 'pending' | 'approved' | 'rejected'
